/**
 * SearchInput — Constants
 *
 * Shared Tailwind class strings.
 */

// ── Layout ───────────────────────────────────────────────────────

export const WRAPPER_CLASSES = "relative flex flex-col";

// ── Shortcut Badge ───────────────────────────────────────────────

export const SHORTCUT_BADGE_CLASSES =
  "inline-flex items-center gap-0.5 rounded bg-bg-secondary text-text-tertiary border border-border-default px-1.5 py-0.5 text-xs font-mono leading-none select-none pointer-events-none shrink-0";

// ── Clear Button ─────────────────────────────────────────────────

export const CLEAR_BUTTON_CLASSES =
  "absolute right-2 top-1/2 -translate-y-1/2 flex items-center justify-center h-6 w-6 rounded text-text-tertiary hover:text-text-primary hover:bg-bg-secondary transition-colors duration-150 focus:outline-none z-10";
