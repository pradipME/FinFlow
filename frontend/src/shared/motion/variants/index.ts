/**
 * Motion Variants — Barrel Export
 *
 * All component-level variant maps in one import.
 * Components: import { buttonVariants } from "@/shared/motion/variants";
 */

// Button
export { buttonVariants, ghostButtonVariants, iconButtonVariants, buttonSpinnerVariants } from "./button";

// Card
export {
  cardVariants,
  cardFeaturedVariants,
  cardChildrenVariants,
  cardChildItem,
  listItemVariants,
} from "./card";

// Dialog
export {
  dialogVariants,
  dialogPanelVariants,
  confirmDialogVariants,
  dialogBackdropVariants,
  dialogStagger,
  dialogChildItem,
} from "./dialog";

// Sidebar
export {
  sidebarVariants,
  sidebarLabelVariants,
  sidebarItemVariants,
  sidebarIconVariants,
  sidebarOverlayVariants,
  sidebarMobileVariants,
} from "./sidebar";

// Navbar
export {
  navbarVariants,
  navbarDarkVariants,
  menuToggleVariants,
  mobileNavVariants,
  navItemVariants,
} from "./navbar";
